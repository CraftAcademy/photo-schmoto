<% @photos.each do |photo| %>
  <div><%= image_tag (photo.image_path) %></div>
  <div>Title: <%= photo.title %></div>
  <div>Price: <%= photo.price %></div>
<% end %>
<% if @photos.empty? %>
  <%="There is no matching picture"%>
<%end%
